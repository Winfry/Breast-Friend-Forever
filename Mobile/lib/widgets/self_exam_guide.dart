
import 'package:flutter/material.dart';

class SelfExamGuide extends StatelessWidget {
  const SelfExamGuide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Self Exam Guide Widget'),
    );
  }
}
